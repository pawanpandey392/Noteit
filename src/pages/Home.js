import Loader from './../components/Loader';

function HomePage() {
  return (
    <section className="mt-3">
      <div>Home Page.</div>
      <Loader />
    </section>
  );
}

export default HomePage;
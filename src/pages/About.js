import Loader from './../components/Loader';

function AboutPage() {
  return (
    <section className="mt-3">
      <div>About Page.</div>
      <Loader />
    </section>
  );
}

export default AboutPage;